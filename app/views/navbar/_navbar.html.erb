<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">ToCrypt2 News</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="<%= root_path %>"><i class="fa fa-home"></i> Accueil</a>
        </li>
        <li class="nav-item">
          <%= link_to "Produits", products_path, class: "nav-link" %>
        </li>
        <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "Articles", posts_path, class: "nav-link" %>
        </li>
        <% end %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Compte
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if user_signed_in? %>
              <%= link_to "Mon Profil", profil_path, class: "dropdown-item" %>
              <%= link_to "Editer les Roles", edit_user_path(current_user), class: "dropdown-item" %>
              <%= link_to "Inviter un(e) ami(e)", new_user_invitation_path, class: "dropdown-item" %>
              <li><%= link_to "Editer: " + current_user.username.capitalize || current_user.username.capitalize, edit_user_registration_path, class: "dropdown-item #{ yield(:user) } text-info bg-dark" %></li>
              <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            <% else %>
              <%= link_to "Connexion", new_user_session_path, :method => 'get', class: "dropdown-item" %>
              <%= link_to "Inscription", new_user_registration_path, :method => 'get', class: "dropdown-item" %>
              
              <li><a class="dropdown-item" href="#">Another action</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="#">Something else here</a></li>
            <% end %>
          </ul>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
        </li>
      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>